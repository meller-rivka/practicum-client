import { GenderTextPipe } from './gender-text.pipe';

describe('GenderTextPipe', () => {
  it('create an instance', () => {
    const pipe = new GenderTextPipe();
    expect(pipe).toBeTruthy();
  });
});
